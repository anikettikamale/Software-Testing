package com.fb;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.chrome.ChromeDriver;
public class chromefacebook {

	public static void main(String args[])
	{
	
		WebDriver driver=new ChromeDriver();
		
		driver.get("https://www.instagram.com");
		
		//driver.close();
		
	}
		
		//WebDriver driver = new ChromeDriver();: This creates a driver object using ChromeDriver. 
	//This object represents the Chrome browser and can control it.

     //driver.get("https://www.facebook.com");: This line tells the driver (the browser) to open the Facebook
	//website by providing its URL.
	
	
	// 1. add maven dependency in pom.xml 
	
	
	
		
	}
